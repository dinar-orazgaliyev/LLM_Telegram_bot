version: '3.9'

services:
  llm_bot:
    build: .
    container_name: llm_telegram_bot
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
    volumes:
      - ./:/app
    restart: unless-stopped
